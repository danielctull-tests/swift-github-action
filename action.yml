name: 'Hello World'
description: 'Run swift command.'
inputs:
  swift-command:
    description: 'Swift command to run'
    required: true
    default: '--version'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.swift-command }}
